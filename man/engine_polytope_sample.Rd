% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polytope.R
\name{engine_polytope_sample}
\alias{engine_polytope_sample}
\title{Sample feasible part-worths from the constraint polytope (Hit-and-Run)}
\usage{
engine_polytope_sample(
  engine,
  n = 500,
  burnin = 200,
  thin = 1,
  method = c("standard", "coordinate"),
  chains = 1,
  parallel = FALSE,
  n_cores = NULL,
  progress = NULL,
  seed = NULL,
  tol = 1e-10,
  jitter = 1e-08
)
}
\arguments{
\item{engine}{A \code{paprika_engine} created by \code{\link[=engine_create]{engine_create()}}.}

\item{n}{Number of samples to return.}

\item{burnin}{Number of initial Hit-and-Run steps to discard.}

\item{thin}{Keep one sample every \code{thin} steps.}

\item{method}{Sampling method: \code{"standard"} (random directions) or
\code{"coordinate"} (axis-aligned, often faster mixing in high dimensions).}

\item{chains}{Number of independent chains to run. If > 1, Gelman-Rubin
convergence diagnostics will be computed.}

\item{parallel}{Logical. Use parallel processing for multiple chains?
Requires \code{parallel} package (base R).}

\item{n_cores}{Number of cores for parallel sampling. Default uses
\code{parallel::detectCores() - 1}.}

\item{progress}{Logical. Show progress bar for long-running sampling?
Auto-enabled when total_steps > 1000.}

\item{seed}{Optional integer seed.}

\item{tol}{Numerical tolerance used for rank and feasibility checks.}

\item{jitter}{Small jitter added to the starting point (projected back to
the equality subspace). Useful when the polytope is lower-dimensional.}
}
\value{
An object of class \code{paprika_polytope} with elements:
\itemize{
\item \code{weights} matrix of sampled part-worth vectors in the original normalization
used by the constraints (alias: \code{w}).
\item \code{weights_scaled} matrix of the same samples rescaled so that the sum of
criterion ranges equals 100 (alias: \code{w_scaled}).
\item \code{var_names} variable names (\code{criterion:level}).
\item \code{start} starting feasible point.
\item \code{chains} (if chains > 1) list of per-chain samples.
\item \code{diagnostics} (if chains > 1) convergence diagnostics (Gelman-Rubin R-hat).
}
}
\description{
Draws samples from the feasible polytope implied by the observed decisions
and the structural constraints (anchors, monotonicity, and normalization).
Two Hit-and-Run variants are available: standard (random directions) and
coordinate (axis-aligned directions). Coordinate H&R often has better mixing
in high dimensions.
}
\details{
\strong{Method Selection:}
\itemize{
\item \code{"standard"}: classic Hit-and-Run with random directions in nullspace.
\item \code{"coordinate"}: cycles through coordinate directions (faster per-iteration,
often better mixing for >10 variables).
}

\strong{Parallel Sampling:}
When \code{chains > 1} and \code{parallel = TRUE}, chains are run in parallel using
\code{parallel::mclapply()} (Unix/Mac) or \code{parallel::parLapply()} (Windows).
Seeds are set deterministically per chain for reproducibility.
}
\examples{
eng <- engine_create(list(Effekt = c("Niedrig", "Mittel", "Hoch"), Risiken = c("Viel", "Wenig")))
# After collecting some decisions:
# s <- engine_polytope_sample(eng, n = 200, method = "coordinate")
# Multi-chain with diagnostics:
# s <- engine_polytope_sample(eng, n = 200, chains = 4, parallel = TRUE)

}
