% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{bootstrap_rank_profiles}
\alias{bootstrap_rank_profiles}
\title{Bootstrap ranking + Fit percentages for arbitrary profiles}
\usage{
bootstrap_rank_profiles(
  boot,
  profiles,
  id_col = NULL,
  top_k = 3L,
  level = 0.95,
  return_pairwise = FALSE
)
}
\arguments{
\item{boot}{A \code{paprika_bootstrap} object.}

\item{profiles}{data.frame with at least the engine criteria columns.}

\item{id_col}{Optional column name in \code{profiles} used as identifier.
If NULL, rownames(profiles) or row index is used.}

\item{top_k}{Integer (e.g., 3 for Top-3 probability).}

\item{level}{CI level for Fit\% (e.g., 0.95).}

\item{return_pairwise}{Logical. If TRUE, returns pairwise P(i > j).}
}
\value{
A list with \code{table} (data.frame) and optionally \code{pairwise} (matrix).
}
\description{
Given a set of profiles (rows) with criterion levels, compute for each profile:
\itemize{
\item Fit\% distribution (0-100 normalized within the model)
\item CI for Fit\%
\item Probability to be rank 1 and rank <= top_k across bootstrap samples
\item Optionally pairwise P(i > j)
}
}
