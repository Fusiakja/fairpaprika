% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{engine_bootstrap}
\alias{engine_bootstrap}
\title{Bootstrap uncertainty for PAPRIKA weights}
\usage{
engine_bootstrap(
  engine,
  B = 200L,
  seed = NULL,
  min_ok = NULL,
  eps_strict = 0,
  tau_equal = NULL,
  parallel = FALSE,
  n_cores = NULL,
  verbose = FALSE,
  progress = NULL
)
}
\arguments{
\item{engine}{A \code{paprika_engine} with \code{decisions}.}

\item{B}{Integer. Number of bootstrap replicates.}

\item{seed}{Optional integer for reproducibility.}

\item{min_ok}{Minimum number of successful replicates required for \code{ok=TRUE}.
Default: max(10, floor(B * 0.5)).}

\item{eps_strict}{Bootstrap strict margin (override). Default 0 (more tolerant).}

\item{tau_equal}{Bootstrap equality band (override). Default uses \code{engine$settings$tau_equal}.}

\item{parallel}{Logical. Use parallel processing? Requires \code{parallel} package (base R).}

\item{n_cores}{Number of cores for parallel processing. Default uses \code{parallel::detectCores() - 1}.}

\item{verbose}{Logical. Print progress every ~50 reps (ignored if parallel = TRUE).}

\item{progress}{Logical. Show progress bar? Auto-enabled for B > 100 (disabled for parallel).}
}
\value{
An S3 object of class \code{paprika_bootstrap}.
}
\description{
Resamples the observed decisions with replacement (nonparametric bootstrap),
re-solves the PAPRIKA linear program for each replicate, and returns a
distribution of part-worths and criterion importances.
}
